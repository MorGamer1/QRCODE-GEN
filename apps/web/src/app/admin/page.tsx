import { ComingSoon } from '@/components/layout/coming-soon';

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin panel"
      description="Oversee users, QR codes, storage, system settings, audit logs, and backups. Coming very soon."
    />
  );
}
