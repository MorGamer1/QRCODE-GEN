## Summary

<!-- What does this change do, and why? -->

## Test plan

<!-- How was this verified? Check what applies. -->

- [ ] `pnpm lint && pnpm typecheck` pass
- [ ] `pnpm test` passes (unit tests)
- [ ] `pnpm --filter @qrgen/api test:e2e` passes (if backend behavior changed)
- [ ] Verified manually in the browser (if UI changed)

## Screenshots

<!-- If this changes the UI, a before/after screenshot saves the reviewer a checkout. -->
